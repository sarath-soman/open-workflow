// The `owf` version. Kept in lockstep with the root package.json by a test
// (the compiled binary can't read package.json at runtime, so this is the
// source of truth that ships in the executable).
export const VERSION = '0.2.0'
